/**
 * @function gussedWordsReducer
 * @param {array} state - Array of guessed words.
 * @param {object} state - Action to be reduced.
 * @returns {array} - new guessedWords state.
 */
export default (action, state) => {
    return null;
}